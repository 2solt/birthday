apiVersion: v1
kind: Service
metadata:
  name: {{ include "birthday.fullname" . }}
  labels:
    {{- include "birthday.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "birthday.selectorLabels" . | nindent 4 }}
